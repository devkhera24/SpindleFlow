# Test: Duplicate agent ID
# Expected: SemanticValidationError
# Error: "Duplicate agent ID found: "researcher""
# Suggestions: Each agent must have a unique ID

agents:
  - id: researcher
    role: Research Analyst
    goal: Conduct research

  - id: researcher
    role: Senior Researcher
    goal: Deep dive analysis

workflow:
  type: sequential
  steps:
    - agent: researcher
