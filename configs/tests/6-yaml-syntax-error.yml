# Test: YAML parsing error (indentation)
# Expected: YAMLParsingError
# Error: "Failed to parse YAML file"
# Suggestions: Check for proper YAML indentation

agents:
  - id: researcher
    role: Research Analyst
    goal: Conduct research
  - id: writer
  role: Content Writer  # Wrong indentation
  goal: Write content

workflow:
  type: sequential
  steps:
    - agent: researcher
